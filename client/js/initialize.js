// Initial data to use in the app
let state = {
  fridgeItems: [],
  recipeBookRecipes: [],
};

function getRecipes() {
  //TODO: get user's saved recipes from database and add to state.recipeBookRecipes
}
